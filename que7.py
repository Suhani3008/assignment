#Write a python program that takes a string as input and returns its length

input_string = input("Enter a String to know its length : ")

string_length = len(input_string)

print("The length of the {} is {}".format(input_string,string_length))